{% extends 'base.html.twig' %}
{% block body %}
    <h1> {{ post.title }}</h1>
    <em>{{ post.category }}</em>
    <hr>
    <img class="img-fluid" src="{{ '/uploads/' ~ post.image }}" alt="">
{% endblock %}